<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="0">
        
        <record id="view_account_move_discount_form" model="ir.ui.view">
            <field name="name">view.account.move.discount.form</field>
            <field name="model">account.move.discount</field>
            <field name="arch" type="xml">
                <form>
                    <sheet>
                        <field name="move_id" invisible="1"/>
                        <field name="company_id" invisible="1"/>
                        <field name="currency_id" invisible="1"/>
                        <field name="account_discount_type" invisible="1"/>
                        <div class="col-12">
                            <group>
                                <field name="discount_mode" widget="radio" options="{'horizontal': True}"
                                       invisible="account_discount_type != 'adevx'"/>
                            </group>
                        </div>
                        <div class="row">
                            <div class="col-sm-5 col-md-4 col-lg-4 col-4">
                                <group>
                                    <label for="discount_amount" string="Discount" invisible="discount_mode != 'amount'"/>
                                    <field name="discount_amount" invisible="discount_mode != 'amount'" nolabel="1"/>
                                    <label for="discount_percentage" string="Discount"
                                           invisible="discount_mode != 'percentage'"/>
                                    <field name="discount_percentage" widget="percentage" nolabel="1"
                                           invisible="discount_mode != 'percentage'"/>
                                </group>
                            </div>
                            <div class="col-sm-7 col-md-8 col-lg-8 col-8">
                                <field name="discount_type" widget="radio"/>
                            </div>
                        </div>
                    </sheet>
                    <footer>
                        <button type="object" string="Apply" name="action_apply_discount" class="btn btn-primary" data-hotkey="q"/>
                        <button special="cancel" string="Discard" class="btn btn-secondary" data-hotkey="x"/>
                    </footer>
                </form>
            </field>
        </record>

    </data>
</odoo>
